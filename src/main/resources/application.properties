# Abilita la console H2 solo se ti serve (opzionale, puoi commentarla)
spring.h2.console.enabled=false

# Server Spring Boot
server.port=8080
server.address=0.0.0.0

# Configurazione datasource MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/login?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=MATTIFUMAfm22245z
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
